<div class="tag-cloud-page">
  <div class="tag-cloud">
    <% site.tags.sort('name').each(function(tag){ %>
      <a class="tag-cloud-item" href="<%- url_for(tag.path) %>"><%= tag.name %><span class="tag-cloud-count"><%= tag.posts.length %></span></a>
    <% }) %>
  </div>
</div>
